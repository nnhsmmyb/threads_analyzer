## `timeline.md`

```markdown
# timeline

| 時刻 | イベント | 根拠 |
|------|----------|------|
| ... | ... | incident_context の該当記述 |
```

### 制約

- 推測・解釈は書かない。事実の並べ替えのみ。
- 各行の `根拠` は `incident_context.md` の `## 既知の事実` に対応する記述を指す。
- 時刻は昇順または降順のいずれかで一貫させる。
